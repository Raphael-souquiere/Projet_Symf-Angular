{% include 'base.html.twig' %}

{% block body %}
    <h1>Edition d'affectation</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('affecte_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" onclick="if(!confirm('Etes vous sure de vouloir supprimer cette affectation ?')) { return false; }">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
{% include 'footer.html.twig' %}
