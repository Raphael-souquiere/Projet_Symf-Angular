{% include 'base.html.twig' %}

{% block body %}
    <h1>Edition d'un type d'utilisateur</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" valu e="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('typeuser_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" onclick="if(!confirm('Etes vous sure de vouloir supprimer ce type d'utilisateur ainsi que tout les utilisateurs qui lui sont associé ?')) { return false; }">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
{% include 'footer.html.twig' %}
